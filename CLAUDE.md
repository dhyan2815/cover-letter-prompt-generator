# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Common Commands
- **Build**: No build step defined. Add a build script (e.g., `npm run build`, `make`, or a language‑specific compile command) and document it here.
- **Lint**: No linter configured. When a linting tool is added (ESLint, flake8, pylint, etc.), include the command such as `npm run lint` or `flake8 .`.
- **Run Tests**: No test suite detected. Once a testing framework is chosen (e.g., pytest, jest, go test), provide the generic command `npm test`, `pytest`, or `go test ./...`. For a single test, include the pattern used by the test runner (e.g., `pytest tests/test_module.py::TestClass::test_name`).
- **Run a Single File**: Execute the main entry point for the project's language, e.g., `python main.py`, `node src/index.js`, `go run ./cmd/app`. Update this section when a concrete entry point is added.

## High‑Level Architecture
The repository currently contains no source files. When code is added, consider documenting the following high‑level aspects here:
- **Language / Runtime**: Indicate the primary language(s) (e.g., Python, JavaScript/TypeScript, Go) and any version requirements.
- **Project Layout**: Describe the top-level directories. This project uses `workflows/`, `visuals/` (excalidraw diagrams), `memories/`, `content/`, `designs/`, `docs/`, and `.claude/` for knowledge base instead of a traditional `src/` layout. Keep the description concise; the file tree can be inspected directly.
- **Core Components**: Identify the main logical components (e.g., API layer, data access layer, business logic, UI) and how they interact. Add a brief diagram if helpful.
- **Dependency Management**: Mention the package manager (e.g., `pip`, `npm`, `go modules`) and where dependency files live (`requirements.txt`, `package.json`, `go.mod`).
- **Configuration**: Note any environment‑variable based configuration or config files (e.g., `.env`, `config.yaml`).
- **Entry Point**: Explain how the application is started (e.g., a `main` module, a CLI entry point, a web server). Update this once the entry point exists.

## Repository‑Specific Rules
- No `.cursor` or Copilot rule files are present. When such files are added, summarize the most important directives here.
- The `README.md` file clarifies that this repository is an experimental playground for CLI tools and AI agents, focusing on Claude Code, Gemini CLI, and n8n.

## Memory System
- A persistent memory system exists at `.claude/memory/`.
- The `MEMORY.md` file in that directory indexes all memory files.
- At the start of each session, I will review `MEMORY.md` and relevant memories to get context.
- Throughout the session, I will update these files with new information about the user, their preferences, project decisions, and other relevant context.

## Documentation System
- The `.claude/docs/` directory contains Claude-specific documentation:
  - **ARCHITECTURE.md**: System design and component relationships
  - **GLOSSARY.md**: Terminology definitions
  - **QUICK_START.md**: Getting started guide

## Coding Standards
- `.claude/rules/` contains project-wide standards:
  - **coding_standards.md**: Universal principles + language-specific guides (Python, JS/TS, Go, Java, C)
  - **naming_conventions.md**: File, class, function, variable, database, and API naming patterns

## How Claude Should Use This File
- Scan this file on startup to learn the available commands and high‑level structure.
- When a user asks how to build, lint, test, or run the project, refer to the concrete commands listed here (or prompt the user to add them if they are missing).
- If the repository gains new top‑level directories or major components, update the *High‑Level Architecture* section accordingly.
- Keep this file terse; avoid duplicating the full file tree or exhaustive lists of files—those can be inspected directly with `ls` or `tree`.

---
*Generated by Claude Code to give future instances a quick reference for repository conventions and common development tasks.*
